---
description: 把 UI 截图转成代码 / 设计规范（vision-mcp）
---

调用 MCP 工具 `ui_to_artifact`，把下面这张 UI 截图转成代码（除非用户另指定，默认 `task: code`；也可生成 prompt / design-spec / description）。请把用户输入里的图片路径识别为 `image.path`：

$ARGUMENTS
